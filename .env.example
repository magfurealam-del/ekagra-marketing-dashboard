# No environment variables are required to run this app.
# Meta credentials (Ads access token, Ad Account ID, Page ID, Page access token)
# are entered at runtime via the lock screen and stored server-side in an
# httpOnly session cookie — never in localStorage, never committed here.
